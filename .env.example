# ─────────────────────────────────────────────────────────────────────────────
# WildfireIQ Kamloops — environment variables
# Copy to `.env` and fill in. Do NOT commit `.env`.
# ─────────────────────────────────────────────────────────────────────────────

# ── Required: the globe will not render without this ────────────────────────
# Cesium Ion access token — for World Terrain, OSM Buildings, Bing imagery.
# Sign up free: https://ion.cesium.com/signin/
VITE_CESIUM_ION_TOKEN=

# ── Required for data ingestion ─────────────────────────────────────────────
# NASA FIRMS MAP_KEY — for satellite hotspots (VIIRS/MODIS).
# Free instant signup: https://firms.modaps.eosdis.nasa.gov/api/map_key
FIRMS_MAP_KEY=

# WAQI / AQICN token — for air-quality cross-check + pollutant split.
# Free signup: https://aqicn.org/data-platform/token
WAQI_TOKEN=

# ── Optional: the in-app assistant ──────────────────────────────────────────
# OpenRouter key. Without it /api/assistant reports `configured: false` and the
# frontend hides its launcher; everything else works unchanged.
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=
# Model to route to. GLM 5.3 Flash is the default: tool calling, a 1.3M-token
# context, and roughly $0.075 / $0.25 per million input / output tokens, which
# puts a typical answer well under a hundredth of a cent.
ASSISTANT_MODEL=z-ai/glm-5.3-flash
# Budget ceilings per question.
ASSISTANT_MAX_STEPS=5
ASSISTANT_MAX_TOOL_CALLS=12

# ── App configuration (defaults are fine for local dev) ──────────────────────
VITE_API_BASE_URL=http://localhost:8000
DATABASE_URL=sqlite+aiosqlite:///./data/wildfireiq.db

# ── Feature flags ────────────────────────────────────────────────────────────
# Section 6 of /climate renders only when this is true AND the campus CSV
# exists at data/tru/tru_campus_energy.csv.
VITE_ENABLE_TRU_CARBON=false
