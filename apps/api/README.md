# WildfireIQ API

FastAPI backend for ingestion, ML inference, and REST endpoints.

```bash
uv sync
uv run uvicorn wildfireiq_api.main:app --reload
```

API docs at http://localhost:8000/docs once running.
